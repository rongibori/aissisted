node_modules
.pnpm-store
.next
.turbo
dist
build
coverage
.env
.env.*
!.env.example
.DS_Store
*.log
*.tsbuildinfo

# Next.js auto-regenerates next-env.d.ts on every build — ignored per
# https://nextjs.org/docs/app/api-reference/config/typescript#ignoring-typescript-errors
apps/*/next-env.d.ts

packages/db/data
# packages/db/drizzle — migration SQL files ARE tracked (needed for production deploys)
apps/api/data/*.db

# AWS bootstrap outputs — generated locally, contains account-specific infra IDs
infra/aws/.bootstrap-output.env
infra/**/.bootstrap-output.env

# Next.js auto-generated type file — regenerated on every build, not source
apps/*/next-env.d.ts

# Cowork coordination layer (local-only; managed by Cowork session)
/jeffrey/
